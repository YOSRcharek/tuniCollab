{% extends 'base.back.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>NomCategorie</th>
                <td>{{ categorie.nomCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_back_index') }}" class="btn btn-primary" style="width: 150px;">Retour à la liste</a>

<a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-info" style="background-color: #00008B; color: white; width: 150px;">Modifier</a>




    {{ include('/back/categorie/_delete_form.html.twig') }}
</div>
{% endblock %}
