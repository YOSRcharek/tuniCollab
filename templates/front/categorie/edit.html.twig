{% extends 'base.html.twig' %}

{% block title %}Edit Categorie{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Categorie</h1>
        
        <table class="table">
            <tbody>
                <tr>
                    <td colspan="2">
                        {{ include('front/categorie/form.html.twig', {'button_label': 'Update'}) }}
                    </td>
                </tr>
                <tr>
                    <td colspan="2">
                        {{ include('front/categorie/_delete_form.html.twig') }}
                    </td>
                </tr>
            </tbody>
        </table>
        
        <a href="{{ path('app_categorie_index') }}" class="btn btn-secondary">Back to list</a>
    </div>
{% endblock %}


