{# templates/type_event/new.html.twig #}

{% extends 'baseAdmin.html.twig' %}

{% block title %}Create TypeEvent{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Create TypeEvent</h1>

        {{ form_start(form, {'attr': {'class': 'form'}}) }}
        <div class="mb-3">
            {{ form_row(form.nom, {'attr': {'class': 'form-control', 'placeholder': 'Enter the type name'}}) }}
        </div>

        <!-- Add more form fields as needed -->

        <button type="submit"  class="btn btn-primary">Save</button>
        {{ form_end(form) }}

        <a href="{{ path('app_type_event_index') }}" class="btn btn-secondary">Back to List</a>
    </div>
{% endblock %}
