{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit TypeEvent{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h1 class="card-title">Edit TypeEvent</h1>
        </div>
        <div class="card-body">
            {{ include('type_event/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <div class="card-footer">
            
            <a href="{{ path('app_type_event_indexadmin') }}" class="btn btn-primary">Back to list</a>
            <a href="{{ path('app_show_eventsadmin') }}" class="btn btn-secondary">Retour à la liste de tous les événements</a>
        </div>
    </div>

    {{ include('type_event/_delete_form.html.twig') }}
{% endblock %}

