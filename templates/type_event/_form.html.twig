
{{ form_start(form) }}

<div class="form-group">
    {{ form_label(form.nom, 'Nom', {'label_attr': {'class': 'font-weight-bold'}}) }}
    {{ form_widget(form.nom, {'attr': {'class': 'form-control custom-border'}}) }}
</div>



<div class="form-group">
    {{ form_widget(form._token, {'attr': {'class': 'form-control custom-border'}}) }}
</div>

<button type="submit" class="btn btn-primary">ajouter type</button>

{{ form_end(form) }}
