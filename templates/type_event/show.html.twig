{% extends 'base.html.twig' %}

{% block title %}TypeEvent{% endblock %}

{% block body %}
    <h1>TypeEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_event.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_event.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_event_index') }}">retour au list</a>
    <a href="{{ path('app_show_events') }}">retour au list de tous les evenements</a>

    <a href="{{ path('app_type_event_edit', {'id': type_event.id}) }}">edit</a>
    

    {{ include('type_event/_delete_form.html.twig') }}
{% endblock %}
