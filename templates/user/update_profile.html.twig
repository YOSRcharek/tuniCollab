{# templates/user/user_profile_form.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Update Profile{% endblock %}

{% block body %}
    <h1>Update Profile</h1>
    {{ form_start(form) }}
    {{ form_row(form.bio) }}
    {{ form_row(form.image) }}
    <button type="submit">Update</button>
    {{ form_end(form) }}
{% endblock %}
